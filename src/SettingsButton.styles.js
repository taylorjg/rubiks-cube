import styled from "@emotion/styled"

export const StyledSettingsIcon = styled.div`
  color: #ffffff;
  opacity: 0.5;
  position: fixed;
  top: 0.5rem;
  left: 0.5rem;
  cursor: pointer;
  display: flex;
  &:hover {
    opacity: 1;
    transform: scale(1.2);
  }
`
