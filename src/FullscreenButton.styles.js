import styled from "@emotion/styled"

export const StyledFullscreenIcon = styled.button`
  color: #ffffff;
  opacity: 0.5;
  position: fixed;
  top: 0.5rem;
  right: 0.5rem;
  cursor: pointer;
  display: flex;
  padding: 0;
  border: none;
  background: none;

  &:hover {
    opacity: 1;
    transform: scale(1.2);
  }
`
