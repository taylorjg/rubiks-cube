import styled from "@emotion/styled"

export const StyledMoveStep = styled.div`
  position: fixed;
  bottom: .5rem;
  left: .5rem;
  font-size: 1.5rem;
  font-weight: 500;
  color: #ffffff;
  letter-spacing: 0.05em;
  pointer-events: none;
  user-select: none;
`
